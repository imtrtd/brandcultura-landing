'use client'
import { useLocale } from './locale-provider'
import { Reveal } from './reveal'
export function Analyse() { const { copy } = useLocale(); const t = copy.analyse; return <section id="analyse" aria-labelledby="analyse-heading" className="mx-auto max-w-6xl px-4 py-14 sm:px-5 sm:py-16 md:px-8 md:py-24"><Reveal className="max-w-3xl"><span className="label-mono text-muted-foreground">{t.label}</span><h2 id="analyse-heading" className="display mt-4 text-pretty text-4xl font-bold text-foreground sm:text-5xl md:text-6xl">{t.title}</h2><p className="mt-5 text-pretty leading-relaxed text-muted-foreground sm:mt-6">{t.intro}</p></Reveal><div className="mt-9 grid grid-cols-1 gap-3 sm:mt-12 md:grid-cols-3">{t.bands.map((band,index) => <Reveal key={band[0]} delay={index*80}><div className="carbon-panel flex min-h-full flex-col gap-3 p-5 sm:p-6 md:p-8"><span className="label-mono text-lime">{band[0]}</span><h3 className="text-xl font-semibold text-foreground">{band[1]}</h3><p className="text-sm leading-relaxed text-muted-foreground">{band[2]}</p></div></Reveal>)}</div></section> }
