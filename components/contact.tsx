'use client'
import { useState } from 'react'
import { useLocale } from './locale-provider'
import { Reveal } from './reveal'
export function Contact() { const [sent,setSent] = useState(false); const { copy } = useLocale(); const t = copy.contact; return <section id="kontakt" aria-labelledby="kontakt-heading" className="carbon-surface border-t border-border"><div className="mx-auto grid max-w-6xl grid-cols-1 gap-9 px-4 py-14 sm:gap-12 sm:px-5 sm:py-16 md:grid-cols-2 md:px-8 md:py-24"><Reveal><span className="label-mono text-lime">{t.label}</span><h2 id="kontakt-heading" className="display mt-4 text-pretty text-4xl font-bold text-foreground sm:text-5xl md:text-7xl">{t.title}</h2><p className="mt-5 max-w-sm text-pretty leading-relaxed text-muted-foreground sm:mt-6">{t.intro} <span className="text-foreground">{t.hours}</span></p></Reveal><Reveal delay={100}>{sent ? <div className="carbon-panel signal-glow flex min-h-56 flex-col justify-center border-lime p-6 sm:p-8"><span className="label-mono text-lime">{t.received}</span><p className="mt-3 text-lg text-foreground">{t.thanks}</p></div> : <form className="carbon-panel flex flex-col gap-4 p-4 sm:p-6" onSubmit={e => {e.preventDefault();setSent(true)}}><Field label={t.name}><input type="text" required placeholder={t.namePlaceholder} className="min-h-12 border border-border bg-background/80 px-4 py-3 text-base text-foreground outline-none placeholder:text-muted-foreground/60 focus:border-lime"/></Field><Field label={t.email}><input type="email" required placeholder="you@sound.de" className="min-h-12 border border-border bg-background/80 px-4 py-3 text-base text-foreground outline-none placeholder:text-muted-foreground/60 focus:border-lime"/></Field><Field label={t.message}><textarea rows={4} placeholder={t.messagePlaceholder} className="resize-none border border-border bg-background/80 px-4 py-3 text-base text-foreground outline-none placeholder:text-muted-foreground/60 focus:border-lime"/></Field><button type="submit" className="mt-2 min-h-12 bg-lime px-6 py-3 text-sm font-semibold text-lime-foreground transition-opacity hover:opacity-90">{t.send}</button></form>}</Reveal></div></section> }
function Field({label,children}:{label:string;children:React.ReactNode}) { return <label className="flex flex-col gap-2"><span className="label-mono text-muted-foreground">{label}</span>{children}</label> }
